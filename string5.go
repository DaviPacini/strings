//Peça ao usuário para digitar uma string e um número n e retorne a mesma string com as
//n primeiras letras em maiúsculo.

package main

import "fmt"

func main() {

	var (
		str string
		num int
	)
	fmt.Println("Informe uma string e um numero: ")
	fmt.Scan(&str)
	fmt.Scan(&num)
	for i := 0; i < len(str); i++ {

	}
}
